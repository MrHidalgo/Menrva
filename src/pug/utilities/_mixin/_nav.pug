//- NAV
//- ========================

    @param {String} classMod
    @param {Object Array} arrData - value: [href, name]
    @param {Object Array} arrData - value: [href, iconName, name]

    @example
      +nav('header', arr)
      +navIcon('header', arr)

//- ========================
mixin nav(classMod, arrData, activeBool)
  nav.nav(class!= "nav--" + classMod)
    ul.nav__list
      each val, idx in arrData
        li.nav__item(class!= "nav__item-" + idx)
          a.nav__link(
            href!= arrData[idx].href
            title=""
            class!= (activeBool && idx === 0) ? "is-active nav__link-" + idx: "nav__link-" + idx
          )!= arrData[idx].name

mixin navImage(classMod, arrData, activeBool)
  nav.nav(class!= "nav--" + classMod)
    ul.nav__list
      each val, idx in arrData
        li.nav__item(class!= "nav__item-" + idx)
          a.nav__link(
            href!= arrData[idx].href
            title=""
            class!= (activeBool && idx === 0) ? "is-active nav__link-" + idx: "nav__link-" + idx
          )
            .nav__link-image
              img(src= arrData[idx].image srcset= arrData[idx].image2x alt="" title="")
            span.nav__link-text!= arrData[idx].name

mixin navIcon(classMod, arrData, activeBool)
  nav.nav(class!= "nav--" + classMod)
    ul.nav__list
      each val, idx in arrData
        li.nav__item(class!= "nav__item-" + idx + " nav__item-" + arrData[idx].icon)
          a.nav__link(
            href!= arrData[idx].href
            title=""
            class!= (activeBool && idx === 0) ? "is-active nav__link-" + idx: "nav__link-" + idx
          )
            .nav__link-icon
              i.icon-font(class!= "icon-" + arrData[idx].icon)
            span.nav__link-text!= arrData[idx].name
